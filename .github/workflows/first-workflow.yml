# this is first workflow of actions.
name: my-first-flow
on : push

jobs:
    first-job:
        runs-on: ubuntu-latest   
        steps:
        - name: checkout action
          uses: actions/checkout@v6

        - name: welcome to multiline-actions steps
          run: |
           echo "welcome to multi-line actions"
           ls -tlra
           cat README.md
        - name: Generate ASCII Artwork
          run: cowsay -f dog "Run for honest, I am a DOG" >> dog.txt
             
          
          
